<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/menu_stats"
        android:icon="@drawable/ic_euros"
        android:title="menu_stats"
        app:showAsAction="always"/>

    <item
        android:id="@+id/menu_liste"
        android:icon="@android:drawable/ic_menu_sort_by_size"
        android:title="menu_liste"
        app:showAsAction="always"
        android:onClick="showCarsList"/>

    <item
        android:id="@+id/menu_ajout"
        android:icon="@android:drawable/ic_input_add"
        android:title="menu_ajout"
        app:showAsAction="always"
        android:onClick="showAddCar"/>

</menu>